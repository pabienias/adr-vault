import type { ReactNode } from 'react';

export default function Home(): ReactNode {
	return (
		<main className="flex flex-1 items-center justify-center">
			<h1 className="text-4xl font-bold">ADR Vault</h1>
		</main>
	);
}
